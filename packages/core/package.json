{
  "name": "@katmer/core",
  "version": "0.0.5-alpha.0",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "npm"
  ],
  "napi": {
    "binaryName": "katmer-core"
  },
  "scripts": {
    "build": "napi build --platform --release --features node --target-dir target --no-js --dts native.d.ts",
    "build:debug": "napi build --platform --features node --target-dir target --no-js --dts native.d.ts",
    "artifacts": "napi artifacts",
    "version": "napi version"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.0"
  },
  "engines": {
    "node": ">= 10"
  },
  "publishConfig": {
    "access": "public"
  }
}
